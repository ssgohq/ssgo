// Code scaffolded by ssgo. Safe to edit.

package svc

// BaseServiceContext holds shared dependencies available to all transports.
// Embed this in transport-specific ServiceContext structs.
type BaseServiceContext struct {
	// Add shared dependencies here (e.g., DB connections, cache clients).
	// These will be available to both API and RPC handlers.
}
